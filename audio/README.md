# Intro to DeFi - Audio (Deprecated)

Media is read from the sibling `podcasts/` folder. The upload script (`tools/upload_media.py`) uses `content/podcasts/` as the source for .m4a files, not this `audio/` folder. This folder is unused and can be removed.
