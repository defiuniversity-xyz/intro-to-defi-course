# Intro to DeFi - Videos

This folder contains 12 video files (`.mp4`) for upload to Google Cloud Storage. The upload script (`tools/upload_media.py`) reads from here directly—no moving or renaming required.

**File format:** `lessonN Title_With_Underscores.mp4` (e.g., `lesson1 What_is_DeFi_.mp4`). The script extracts the lesson number from the filename.
